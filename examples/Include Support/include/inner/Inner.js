/**
 * You can import scripts with the include tag
 */
println("Inside Inner.js");
include("{script}/../Outer.js");
Assertions.assertTrue(outer);
innerProcessedCorrectly = true
var a = 1;
var b = a +1;
var secondRound;
if (typeof firstRound === "undefined"){
    firstRound = true;
}else {
    secondRound=true;
}